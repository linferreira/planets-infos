import styled from "styled-components";

export const ImageContainer = styled.figure`
    grid-area: IMG;
    display: grid;
    justify-content: center;
    margin-top: 15vh;

    & img {
        width: 400px;
    }
`